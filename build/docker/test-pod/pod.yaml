apiVersion: v1
kind: Pod
metadata:
  name: go-app
  labels:
    app: go-app
spec:
  containers:
    - name: go-app
      image: yagoninja/test-curl:latest  
      ports:
        - containerPort: 8080

